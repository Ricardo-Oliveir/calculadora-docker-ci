// jest.config.js
module.exports = {
    testEnvironment: 'jsdom',
    // Se você tiver outras configurações do Jest, adicione-as aqui.
    // Por exemplo, para cobertura, o comando npm test --coverage já cuida.
    // No entanto, se quiser que a cobertura seja sempre ativada, pode adicionar:
    // collectCoverage: true,
    // coverageDirectory: 'coverage',
  };